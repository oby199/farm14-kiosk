// Configuration for Farm 14 Kiosk
const config = {
  // API key removed for security
  openaiApiKey: 'REDACTED',
  model: 'gpt-4-turbo-preview',
  temperature: 0.7,
  maxTokens: 500
};

// Export the configuration
export default config; 